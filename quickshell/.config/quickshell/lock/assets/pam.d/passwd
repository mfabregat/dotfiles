# Lock-screen PAM service (bundled — no system PAM edits needed).
# Password only, no fingerprint. Deliberately WITHOUT pam_faillock: a
# wrong password on the lock screen must never lock the account out.
auth required pam_unix.so
